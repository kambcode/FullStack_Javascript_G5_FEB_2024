// Partes de un algoritmo
//        Input(a,b)
function add(a, b) {
  const result = a + b; // Process
  return result; // Output
}

const result = add(2, 2); // 4

console.log(result);

// Medición area triangulo

function areatriangulo(base, altura) {
  return (base * altura) / 2;
}

const base = 5;
const altura = 5;

// const base = 3;
// const altura = 2;

console.log(areatriangulo(base, altura));
